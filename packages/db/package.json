{
    "name": "@ctf/db",
    "version": "0.0.1",
    "private": true,
    "main": "src/index.js",
    "scripts": {
        "dev": "docker run --rm --name postgres --env-file=./env -p 5432:5432 postgres"
    }
}
